[workspace]
members = [
    "crates/gamlastan",
    "crates/gamlastan-actix",
    "crates/gamlastan-mdq",
    "spid-sp-test",
    "example-idp",
]
resolver = "2"

[workspace.package]
version = "0.8.0"
edition = "2021"
repository = "https://github.com/kushaldas/gamlastan"
readme = "README.md"
license = "BSD-2-Clause"
rust-version = "1.88"

[workspace.dependencies]
# Internal crates
gamlastan = { version = "0.8.0", path = "crates/gamlastan" }
gamlastan-actix = { version = "0.8.0", path = "crates/gamlastan-actix" }
gamlastan-mdq = { version = "0.8.0", path = "crates/gamlastan-mdq" }

# XML parsing
uppsala = "0.9.0"

# XML security
# 0.8.0 tracks uppsala 0.9 and the kryptering 0.5 backend. Keep the stack on
# one bergshamra minor to avoid duplicate XML security crates in the lockfile.
bergshamra = "0.8.0"
bergshamra-core = "0.8.0"
bergshamra-dsig = "0.8.0"
bergshamra-enc = "0.8.0"
bergshamra-c14n = "0.8.0"
bergshamra-crypto = "0.8.0"
bergshamra-keys = "0.8.0"

# HSM / PKCS#11 signing backend (the low-level crypto provider beneath
# bergshamra). This direct dependency exists so gamlastan can re-export the
# shared `kryptering::Signer` trait/types without version drift from the
# bergshamra stack. Features mirror bergshamra's kryptering dependency so the
# crate resolves to a single instance with a unified feature set.
kryptering = { version = "0.5.0", default-features = false, features = ["legacy", "post-quantum", "pkcs11"] }

# Date/time
chrono = { version = "0.4", default-features = false, features = ["std", "clock"] }

# Byte buffers
bytes = "1"

# Error handling
thiserror = "2"

# Encoding
base64 = "0.22"
flate2 = "1"

# Legacy digest compatibility. Keep MD5 out of defaults; it is only used by
# explicit migration modes such as PySAML2 EPTID compatibility.
md-5 = "0.10"

# Serialization
serde = { version = "1", features = ["derive"] }

# Web framework
actix-web = { version = "4", features = ["rustls-0_23"] }

# TLS
rustls = "0.23"

# Logging
env_logger = "0.11"
log = "0.4"

# HTTP client
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"] }

# Tokio
tokio = { version = "1", features = ["full"] }
